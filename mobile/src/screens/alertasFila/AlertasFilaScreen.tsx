import React from 'react';
import { EmDesenvolvimento } from '../../components/EmDesenvolvimento';

/** Seção "Alertas de Fila" (a desenvolver). */
export function AlertasFilaScreen(): React.ReactElement {
  return (
    <EmDesenvolvimento
      icone="alert-circle-outline"
      titulo="Alertas de Fila"
      descricao="Avisos de filas/caixas que precisam de atenção. Esta seção será desenvolvida em breve."
    />
  );
}

export default AlertasFilaScreen;
