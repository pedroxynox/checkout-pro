{
  "name": "gestao-frente-de-caixa-stok-center",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo do aplicativo de Gestão de Frente de Caixa - Stok Center (backend NestJS + app móvel Expo).",
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "build": "npm run build --workspace=backend",
    "start": "npm run start --workspace=backend",
    "lint": "npm run lint --workspace=backend",
    "test": "npm run test --workspace=backend"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
